# Tool Decision Tree Reference

This skill's tools are referenced in the main TOOL_DECISION_TREE.md:

- **Section 2.1**: HITL confirm run → `run_and_heal`
- **Section 2.3**: Direct run (no modification) → `run_and_heal`
- **Section 2.4**: Scenario test chain → `run_and_heal` (after modifications)
- **Section 3.3**: After `run_and_heal` → summarize (optionally auto-fix + re-run)
- **Section 3.5**: `monitor_simulation` and `stop_simulation` (via LLM choice)

See the main document at `../../../TOOL_DECISION_TREE.md` (simulator_agent root) for complete routing logic.

