# Tool Decision Tree Reference

This skill's tools are referenced in the main TOOL_DECISION_TREE.md:

- **Section 2.5**: Keyword / documentation question flow using `simulator_manual` → `simulator_examples`
- **Section 2.4**: Scenario test chain using `simulator_manual` and `simulator_examples` for keyword context
- **Section 3.4**: Keyword chain after `simulator_manual` / `simulator_examples` tool calls

See the main document at `../../../TOOL_DECISION_TREE.md` (simulator_agent root) for complete routing logic.

