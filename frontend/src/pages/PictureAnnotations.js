import React from "react";

export default function PictureAnnotations() {
  return (
    <div
      style={{
        padding: "1rem",
        background: "#d9d9dd",
        borderRadius: "8px",
        color: "#202124",
      }}
    >
      <h2 style={{ marginTop: 0 }}>Picture Annotations</h2>
      <p>This page is blank for now.</p>
    </div>
  );
}
