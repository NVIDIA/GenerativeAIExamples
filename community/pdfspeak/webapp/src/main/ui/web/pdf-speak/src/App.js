import React from 'react';
import Home from './pages/Home'; // Adjust the path as needed to import Home component

const App = () => {
    return (
        <div>
            <Home/>
        </div>
    );
};

export default App;
